# Configurações do Servidor
server.port=8080

# Nome do Projeto
spring.application.name=backend

# Configurações do Banco de Dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/zelatech_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=123456

# Configurações do JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Configurações do JWT (Chave secreta - Em produção usar variáveis de ambiente!)
jwt.secret=S3cr3tKeyP4raOZelaTechQueDeveriaSerMaisL0ng4EComplexa!!
jwt.expiration=7200000
